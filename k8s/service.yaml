apiVersion: v1
kind: Service

metadata:
  name: gestion-financiera-service

spec:

  selector:
    app: gestion-financiera

  ports:

    - protocol: TCP
      port: 80
      targetPort: 8080

  type: LoadBalancer